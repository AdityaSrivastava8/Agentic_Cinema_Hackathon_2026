# Copy these values into your hosting provider's secret manager.
# Never commit a real secrets.toml file.

GEMINI_API_KEY = ""
GOOGLE_API_KEY = ""
GCP_PROJECT = ""
GCP_LOCATION = "us-central1"
FIRESTORE_ENABLED = "false"

# Optional service-account object for Firestore:
# gcp_service_account = { type = "service_account", project_id = "..." }
